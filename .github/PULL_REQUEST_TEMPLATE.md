## What this changes

<!-- And why. If it fixes a bug, say what the bug was — the PR is often the
     only place a future reader will find that. -->

## Verification

- [ ] `npm run typecheck`
- [ ] `npm run lint`
- [ ] `npm test`
- [ ] `npm run build`

<!-- If you changed anything the Claude Code plugin ships, say how you verified
     it in its installed shape, not just from the source tree. See
     CONTRIBUTING.md — the source tree has node_modules beside the code and a
     real install does not. -->

## If this touches data, privacy or the user's repositories

- [ ] Nothing new is written into a user's source repository
- [ ] Nothing new can reach a file Project Brain persists without passing the
      secret scanner
- [ ] Profile isolation is unaffected
- [ ] Existing data written by an older version still loads

<!-- Delete this section if none of it applies. -->
