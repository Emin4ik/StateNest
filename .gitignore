node_modules/
dist/
dist-plugin/
coverage/
*.tsbuildinfo
.DS_Store
.env
.env.*
*.log
.vitest-temp/
tests/.tmp/

# Recordings are regenerated with vhs rather than committed - see
# scripts/recording/zero-touch.tape. The one exception is the launch demo the
# README embeds, which has to be in the repository for GitHub to render it.
docs/assets/*.gif
docs/assets/*.mp4
docs/assets/*.webm
!docs/assets/zero-touch.gif
